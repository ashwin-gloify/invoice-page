import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  headerMain: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 10,
  },
  textStyle: {
    color: '#000',
    fontSize: 17,
    fontWeight: '500',
  },
  headerItem: {
    padding: 10,
  },
});

export default styles;
